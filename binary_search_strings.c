#include <stdio.h>
#include <string.h>

int bsearch() {
    
}

int main() {
    char arr[][10] = {"apple", "banana", "cherry", "date", "fig", "grape", "kiwi"};
    int n = sizeof(arr) / sizeof(arr[0]);
    
    return 0;
}
